# Downloaded model artifacts — fetched by ../download_model.sh, never committed
# (weights are large; this Jetson-local dir is populated at deploy time).
*.pth
*.pt
*.onnx
*.tflite
*.jpg
model_config.json
class_names.json
class_names.json.orig
