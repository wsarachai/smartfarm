# PlatformIO
.pio/
.pioenvs/
.piolibdeps/
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch/

# Secrets — real WiFi/OTA credentials live here. NEVER commit.
# (include/secrets.example.h IS tracked as the template.)
include/secrets.h

# Generated sdkconfig — derived from sdkconfig.defaults at build time.
# (sdkconfig.defaults IS tracked; the generated ones are not.)
sdkconfig
sdkconfig.old
sdkconfig.esp32dev
sdkconfig.*.old

# Build artifacts
*.bin
*.elf
