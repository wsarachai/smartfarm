# Build output
/build/
*.o

# Vendored single header (fetch per third_party/nlohmann/README.md)
third_party/nlohmann/json.hpp

# Live config is machine-local (installed to /etc/edge-ctrl/), never committed
config.json

# Environment configuration files
.env
.env.local

# Python bytecode (host provisioning scripts)
__pycache__/
*.pyc
