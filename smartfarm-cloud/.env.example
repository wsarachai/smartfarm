# Docker Compose reads this automatically (as `.env`, in the same directory
# as docker-compose.yaml) to fill in ${DASHBOARD_MQTT_PASSWORD}. Copy this to
# `.env` and fill in the real password — the one set for the
# `dashboard-backend` MQTT user (see docs/mqtt-cloud-bridge.md).
DASHBOARD_MQTT_PASSWORD=changeme
